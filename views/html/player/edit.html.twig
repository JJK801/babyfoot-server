{% extends '../base.html.twig' %}

{% block content %}
{{ parent() }}
<form action="" method="post">
    <ul>
        <li>
            <label>Nom:</label>
            <input type="text" name="name[last]" value="{{ Player.name.last }}" />
        </li>
        <li>
            <label>Prénom:</label>
            <input type="text" name="name[first]" value="{{ Player.name.first }}" />
        </li>
        <li>
            <label>Date de naissance:</label>
            <input type="date" name="birth" value="{{ Player.birth|date("Y-m-d") }}" />
        </li>
    </ul>
    <button type="submit">Register</button>
</form>
{% endblock %}